<odoo>
    <record id="account_move_form_inherit_bank_proof" model="ir.ui.view">
        <field name="name">account.move.form.inherit.bank.proof</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='ref']" position="after">
                <field name="waybill_bill_number" readonly="1" invisible="move_type != 'in_invoice'"/>
                <field name="handover_id" readonly="1" invisible="move_type != 'in_invoice' or not handover_id"
                       options="{'no_create': True, 'no_open': True}"/>
                <field name="clearance_id" readonly="1" invisible="move_type != 'in_invoice' or not clearance_id"
                       options="{'no_create': True, 'no_open': True}"/>
                <field name="bank_proof_attachment_ids"
                       widget="many2many_binary"
                       string="Bank Proof (Water Slip)"
                       readonly="is_operation_payment_confirmed"
                       invisible="move_type != 'in_invoice'"/>
                <field name="is_operation_payment_confirmed" readonly="1"
                       invisible="move_type != 'in_invoice'"/>
            </xpath>

            <xpath expr="//header//button[@name='button_draft']" position="after">
                <button name="action_confirm_paid_sync_operation"
                        type="object"
                        string="Confirm Paid"
                        class="btn-success"
                        invisible="move_type != 'in_invoice'
                               or payment_state != 'paid'
                               or not bank_proof_attachment_ids"/>
                <!--            <button name="action_push_customer_invoice_files_to_period"-->
                <!--            type="object"-->
                <!--            string="Push Files To Statement Period"-->
                <!--            class="btn-primary"-->
                <!--            invisible="move_type != 'out_invoice' or not attachment_ids"/>-->
            </xpath>

        </field>
    </record>
    <record id="view_in_invoice_bill_list_inherit_waybill_bill_number" model="ir.ui.view">
        <field name="name">account.move.in.invoice.bill.list.inherit.waybill.bill.number</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_in_invoice_bill_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='invoice_origin']" position="after">
                <field name="waybill_bill_number" optional="show"/>
                <field name="handover_id" optional="show" readonly="1"
                       options="{'no_create': True, 'no_open': True}"/>
                <field name="clearance_id" optional="show" readonly="1"
                       options="{'no_create': True, 'no_open': True}"/>
            </xpath>
        </field>
    </record>
    <record id="account_move_form_inherit_charge_item" model="ir.ui.view">
        <field name="name">account.move.form.inherit.charge.item</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='invoice_line_ids']/list/field[@name='product_id']" position="attributes">
                <attribute name="optional">hide</attribute>
            </xpath>
            <xpath expr="//field[@name='invoice_line_ids']/list/field[@name='product_id']" position="after">
                <field name="charge_item_id" optional="show" readonly="parent.state != 'draft'"
                       options="{'no_create': True, 'no_open': True}"/>
            </xpath>
        </field>
    </record>

</odoo>
