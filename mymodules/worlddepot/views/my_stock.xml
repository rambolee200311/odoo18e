<odoo>
    <!-- Stock Picking Form View -->
    <record id="view_stock_picking_form_inherit" model="ir.ui.view">
        <field name="name">stock.picking.form.inherit</field>
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="stock.view_picking_form"/>
        <field name="arch" type="xml">
            <!-- Add Reverse Validate button in header when picking is done -->
            <xpath expr="//header" position="inside">
                <!--<button name="button_reverse_validate" type="object" string="Reverse Validate" class="btn-secondary" invisible="state!='done'" groups="stock.group_stock_manager,base.group_system"/>-->
                <button name="action_force_validate" type="object" string="Force Validate" class="btn-secondary" invisible="state=='done'" groups="stock.group_stock_manager,base.group_system"/>          
            </xpath>
            <xpath expr="//sheet/group" position="inside">
                <group col="2">
                    <group col="2">
                        <field name="bill_of_lading" invisible="picking_type_code!='incoming'"/>
                        <field name="cntrno" invisible="picking_type_code!='incoming'"/>
                    </group>
                    <group col="2">
                        <field name="ref_1"/>
                        <field name="ref_2"/>
                        <field name="planning_date"/>
                        <field name="load_ref" invisible="picking_type_code=='incoming'"/>
                    </group>
                </group>
            </xpath>
            <!--<xpath expr="//header" position="inside">
                <button name="delete_done_pickings_confirm" type="object" string="Delete Done Pickings" class="btn-danger" />
            </xpath>-->
        </field>
    </record>

    <!-- Stock Lot Form View -->
    <record id="view_stock_lot_form_inherit" model="ir.ui.view">
        <field name="name">stock.lot.form.inherit</field>
        <field name="model">stock.lot</field>
        <field name="inherit_id" ref="stock.view_production_lot_form"/>
        <field name="arch" type="xml">
            <xpath expr="//sheet/group[@name='main_group']" position="inside">
                <group>
                    <field name="bill_of_lading"/>
                    <field name="cntrno"/>
                </group>
            </xpath>
        </field>
    </record>
    <!-- Stock Move Tree View -->
    <record id="view_stock_move_tree_inherit" model="ir.ui.view">
        <field name="name">stock.move.tree.inherit</field>
        <field name="model">stock.move</field>
        <field name="inherit_id" ref="stock.view_move_tree"/>
        <field name="arch" type="xml">
            <field name="product_uom_qty" position="after">
                <field name="nine_digit_linglong_code"/>
            </field>
        </field>
    </record>

    <!-- Inherit stock.location form to show 'is_bonded' -->
    <record id="view_stock_location_form_inherit_worlddepot" model="ir.ui.view">
        <field name="name">stock.location.form.worlddepot.inherit</field>
        <field name="model">stock.location</field>
        <field name="inherit_id" ref="stock.view_location_form"/>
        <field name="arch" type="xml">
            <!-- Insert a small group with the is_bonded checkbox near the top of the sheet -->
            <xpath expr="//sheet" position="inside">
                <group>
                    <field name="is_bonded"/>
                    <field name="location_type" string="Extra Type"/>
                </group>
            </xpath>
        </field>
    </record>
    <!-- Inherit stock.location tree to show 'is_bonded' in list/tree view -->
    <record id="view_stock_location_tree_inherit_worlddepot" model="ir.ui.view">
        <field name="name">stock.location.tree.worlddepot.inherit</field>
        <field name="model">stock.location</field>
        <field name="inherit_id" ref="stock.view_location_tree2"/>
        <field name="arch" type="xml">
            <xpath expr="//list" position="inside">
                <field name="is_bonded"/>
            </xpath>
        </field>
    </record>
</odoo>